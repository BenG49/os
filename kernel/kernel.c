#include "drivers/vga.h"

int main()
{
    u16 temp = get_cursor_pos();
    // clear_screen();
    // print_at("X", 1, 6, WHITE_BLACK);
    // print_at("This text spans multiple lines", 75, 10, RED_WHITE);
    // print_at("There is a line\nbreak", 0, 20, WHITE_BLACK);
    // print("There is a line\nbreak", RED_WHITE);
    // print_at("What happens when we run out of space?", 45, 24, WHITE_BLACK);
}
